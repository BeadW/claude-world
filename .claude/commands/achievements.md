Query the Claude World game for unlocked achievements and display them to the user.

Run this command to get the achievements:
```
python3 $CLAUDE_PROJECT_DIR/hooks/game_client.py achievements
```

Display all unlocked achievements and the total counts for tools used and agents spawned.
