Query the Claude World game for your current status and display it to the user.

Run this command to get the status:
```
python3 $CLAUDE_PROJECT_DIR/hooks/game_client.py status
```

Display the results in a nicely formatted way. If the game is not running, let the user know.
